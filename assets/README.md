# Assets

Drop your real files here — the site already links to them, nothing else needs to change.

- `Ilaria-Nissotti-Revel-CV.pdf` — your CV/résumé. Linked from the nav "résumé" button and the
  contact section's "Download CV" card in [index.html](../index.html).
- (optional) a profile photo, if you want to replace the code-terminal graphic in the About
  section with a picture of yourself. Not currently wired in — say the word and it's a quick change.

Until `Ilaria-Nissotti-Revel-CV.pdf` exists here, those two download links will 404 — add the
file whenever your CV is ready.
